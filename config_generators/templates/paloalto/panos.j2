{# ============================================================
   Template: legacy.paloalto.panos
   Vendor: Palo Alto Networks
   Platform: PAN-OS
   Feature: system.legacy
   Description: Legacy guarded command fragment retained for backward compatibility.
   Required Variables:
     - none: exact commands are supplied by a feature guard.
   Optional Variables:
     - commands: list[string] - exact guard-approved commands (default: []).
     - decision_ids: list[string] - design decision references (default: []).
     - secret_references: list[string] - SecretManager references only (default: []).
   Min Version: model/version scope must be supplied by the caller
   Evidence: https://docs.paloaltonetworks.com/ngfw/pan-os-cli-quick-start/cli-command-hierarchy
   Last Validated: registry-scope-only; authoritative model/version validation required
   ============================================================ #}
{# Commands are supplied by the guarded design input; this template performs no command substitution. #}
{% for command in commands %}{{ command }}
{% endfor %}
