# Sub-Prompt Guidelines

Each sub-prompt is self-contained, has a bounded token estimate, declares required context, lists files, and has an explicit send order. Imports from earlier sub-prompts and outputs consumed by later sub-prompts must be recorded.
