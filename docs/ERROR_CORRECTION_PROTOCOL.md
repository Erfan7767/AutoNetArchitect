# Error Correction Protocol

Errors are classified as syntax, import, type mismatch, missing implementation, wrong vendor command, test failure, or logic error. The protocol selects regeneration or patching and supplies the minimum required context.
